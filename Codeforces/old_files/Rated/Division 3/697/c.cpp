#include<bits/stdc++.h>
#define int long long
using namespace std;

signed main(){
	cin.tie(0)->sync_with_stdio(0);
        int t; cin>>t;
        while(t--){
                int a,b,k; cin>>a>>b>>k;
                map<int, int> fa, fb;
                int a[k], b[k];
                int ans=1;
                for(int i=0; i<k; i++){ cin>>a[i]; fa[a[i]]++; }
                for(int i=0; i<k; i++){ cin>>b[i]; fb[b[i]]++; }
                for(int i=0; i<k; i++){
                        fb[b[i]]
                }

        }
}
