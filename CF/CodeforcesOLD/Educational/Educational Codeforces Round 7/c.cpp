#include <bits/stdc++.h>
#define pb push_back
#define INF (int)1e9
#define endl "\n"
typedef long long ll;
using namespace std;
struct p{
    int f; int s; int x;
};
int main(){
    cin.tie(0)->sync_with_stdio(0);
    int n,m; cin>>n>>m;
    int a[n];
    for(int q=0; q<n; q++){
        cin>>a[q];

    }
    p q[m];
    for(int i=0; i<m; i++)
        cin>>q[i].f>>q[i].s>>q[i].x;

return 0;
}
