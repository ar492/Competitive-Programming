#include <bits/stdc++.h>
#define pb push_back
#define F first
#define S second
#define INF (int)1e9
#define MOD (int)(1e9+7)
#define endl "\n"
typedef long long ll;
using namespace std;

int main(){
//    cin.tie(0)->sync_with_stdio(0);
//    ll nr, nb, ng;
//    cin>>nr>>nb>>ng;
//    priority_queue<ll> r, b, g;
//    for(int i=0; i<nr; i++){
//        int u; cin>>u; r.push(u);
//    }
//    for(int i=0; i<nb; i++){
//        int u; cin>>u; b.push(u);
//    }
//    for(int i=0; i<ng; i++){
//        int u; cin>>u; g.push(u);
//    }
//    ll cnt=0;
//    while(!((nb==0 && nr==0) || (nb==0 && ng==0) || (nr==0 && ng==0))){
//        int rtop=-INF; int btop=-INF; int gtop=-INF;
//        if(nr>=1) rtop=r.top();
//        if(nb>=1) btop=b.top();
//        if(ng>=1) gtop=g.top();
//        //cout<<"rtop, btop, gtop: "<<rtop<<" "<<btop<<" "<<gtop<<endl;
//        if(rtop+btop >= rtop+gtop && rtop+btop >= btop+gtop){
//            cnt+=rtop*btop;
//            nr--; nb--;
//            r.pop(); b.pop();
//        }
//        else if(rtop+gtop >= rtop+btop && rtop+gtop >= btop+gtop){
//            cnt+=rtop*gtop;
//            nr--; ng--;
//            r.pop(); g.pop();
//        }
//        else if(btop+gtop >= rtop+gtop && btop+gtop >= rtop+btop){
//            cnt+=btop*gtop;
//            nb--; ng--;
//            b.pop(); g.pop();
//        }
//    }
//    cout<<cnt<<endl;

return 0;
}
