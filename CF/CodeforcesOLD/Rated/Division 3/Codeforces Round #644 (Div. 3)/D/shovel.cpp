#include <iostream>
#include <fstream>
#include <cmath>
using namespace std;

ifstream fin("shovel.in");

int binarySearch(long long k, long long x)
{
    if (pow(x, x)<k){
        if (pow(x+1, x)>k){
            return
        }
    }

    else if (pow(x, x)>k){

    }
}

int main(){
    int t;
    long long n, k;
    fin>>t;
    for (int i=0; i<t; i++){
        fin>>n>>k;
    }

return 0;
}
